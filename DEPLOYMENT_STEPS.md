# Deployment Steps

From your local repository:

```bash
cd ~/Documents/CAREER_JOB_POSTDOC/ananthkrishna96.github.io
git status
git add .
git commit -m "Update academic website"
git push origin master
```

Then check:

```text
https://ananthkrishna96.github.io/
```

If changes are not visible immediately, wait a few minutes and use a hard refresh (`Ctrl + Shift + R`).
